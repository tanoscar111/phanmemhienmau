@model IEnumerable<BoolDonationApp.Models.Location>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenDiachi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenDiachi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idLocaltion }) |
            @Html.ActionLink("Details", "Details", new { id=item.idLocaltion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idLocaltion })
        </td>
    </tr>
}

</table>
